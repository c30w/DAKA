# 贡献指南

感谢对 **DAKA** 感兴趣！这是一个个人习惯打卡应用，定位是**纯本地、无后端、无账号**。

## 如何参与

- **提 Bug / 建议**：开 Issue，尽量附上复现步骤、系统版本、截图。
- **提代码**：Fork → 新建分支（`feat/xxx`、`fix/xxx`）→ 向 `main` 提 PR。

## 开发约定

- 语言用 Kotlin，UI 用 Jetpack Compose；新功能优先复用现有的 ViewModel / StateFlow 模式。
- **数据库变更必须写 Room Migration**，不能丢老用户数据（这是红线）。
- 保持「纯本地、无网络、无账号」定位：**不要引入后端 / 网络依赖**。
- 提交信息尽量说明「为什么」而非「改了什么」。
- 涉及发布签名的内容一律走 `local.properties`，不要写进构建脚本或提交密钥。

## 构建

见 [README.md](README.md) 的「构建」与「发布签名」两节。

## 行为准则

请友好、就事论事地交流。尊重不同经验水平的贡献者。
